import { Context as KoaContext } from 'koa'
import * as fs from 'fs'

export async function createSubmission(ctx: KoaContext) : Promise<void> {
    // Create a submission for the data sent across by the user
}